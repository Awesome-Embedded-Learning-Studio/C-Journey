name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# 同一 PR 的新一轮推送取消上一轮
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-examples:
    name: 编译 examples (${{ matrix.cc }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        cc: [gcc, clang]
    steps:
      - uses: actions/checkout@v4
      - name: 安装构建工具
        run: sudo apt-get update && sudo apt-get install -y cmake ninja-build
      - name: 选择编译器
        run: echo "CC=${{ matrix.cc }}" >> $GITHUB_ENV
      - name: 编译所有 examples(硬门)
        run: python3 scripts/build_examples.py

  sanitize:
    name: Sanitizer(ASan + UBSan)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: 安装构建工具
        run: sudo apt-get update && sudo apt-get install -y cmake ninja-build clang
      - name: 用 -fsanitize 编译 examples
        env:
          CC: clang
          CFLAGS: -fsanitize=address,undefined -fno-omit-frame-pointer -g
          LDFLAGS: -fsanitize=address,undefined
        run: python3 scripts/build_examples.py
        # build_examples.py 对所有 examples/**/CMakeLists.txt 硬门编译,
        # sanitize 复用同一脚本,已覆盖全部 examples(含 stage1 位运算、stage4-cmake-lib、stage5-tcp-socket)。

  docs:
    name: 文档(frontmatter + markdownlint)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - name: 安装 PyYAML
        run: pip install pyyaml
      - name: 校验 frontmatter
        run: python3 scripts/validate_frontmatter.py
      - name: markdownlint
        uses: DavidAnson/markdownlint-cli2-action@v18
        with:
          globs: 'documents/**/*.md'

  format-check:
    name: clang-format(examples)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: 安装 clang-format
        run: sudo apt-get update && sudo apt-get install -y clang-format
      - name: 检查 examples 格式
        # 仅 examples/。projects/clib-utilities 待整体过一遍 clang-format 后再纳入(见维护者优化清单)。
        run: |
          git ls-files 'examples/*.c' 'examples/*.h' | xargs clang-format --dry-run --Werror

  static-analysis:
    name: 静态分析(clang-tidy,examples)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: 安装 clang-tidy + cmake
        run: sudo apt-get update && sudo apt-get install -y clang-tidy cmake ninja-build
      - name: 跑 clang-tidy(examples 硬门,阶段4·Ch12 引入)
        run: python3 scripts/clang_tidy_check.py


  coverage:
    name: 覆盖率(gcov/lcov,报告门)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: 安装工具
        run: sudo apt-get update && sudo apt-get install -y cmake ninja-build lcov
      - name: 编译+测试 clib(带 --coverage)
        run: |
          cmake -B build-cov -S projects/clib-utilities \
                -DCMAKE_C_FLAGS="--coverage -g" \
                -DCMAKE_EXE_LINKER_FLAGS="--coverage"
          cmake --build build-cov
          cd build-cov && ctest --output-on-failure && cd ..
      - name: 生成覆盖率报告
        # 报告门:只量化不上阈值断言(clib 当前行覆盖较低,上阈值会立刻红;当前仅量化 clib,examples 待补测试后再纳入。
        #  详见 documents/04-engineering/13-coverage.md 与 15-ci-pipeline.md「报告门 vs 硬门」。)
        run: |
          lcov --capture --directory build-cov --output-file coverage.info \
               --ignore-errors mismatch --rc geninfo_auto_base=1
          lcov --remove coverage.info '/usr/*' --output-file coverage.filtered.info --ignore-errors unused
          lcov --summary coverage.filtered.info
      - name: 上传覆盖率报告
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage.filtered.info
