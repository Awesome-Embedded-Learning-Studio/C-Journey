# 安全策略

这是一个 C **教学**仓库,不涉及生产部署。但若你发现:

- 示例 / 文档中**示范不当的不安全写法**(如把缓冲区溢出、释放后使用当正确做法教),
- CI / 依赖中的安全问题,

请优先通过 **GitHub Security Advisory** 私下报告;不便时也可开带 `security` 标签的 Issue。

我们鼓励在教学中**显式标注**哪些代码是「反面教材」(故意的不安全写法),避免学习者误用。
